name: Governance Check

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  call-governance-check:
    uses: paulkiley/engineering-governance/.github/workflows/governance-check.yml@v1.1.0
    with:
      require-templates: true
      markdownlint: true
      yamllint: true
