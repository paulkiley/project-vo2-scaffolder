name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  call-governance-ci:
    uses: paulkiley/engineering-governance/.github/workflows/reusable-ci.yml@v1.0.0
    with:
      python-version: '3.11'
